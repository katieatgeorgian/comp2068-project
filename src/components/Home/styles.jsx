import styled from 'styled-components';

export default {
  Div: styled.div`
    min-height: 95vh;
    display: flex;
    
    p{
      position: fixed;
      top: 3rem;
      left: 13rem;
    }
  `
};